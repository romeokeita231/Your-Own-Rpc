# 推荐答案

## 在项目中使用了什么设计模式，举例说明。

**1）代理模式：**

我采用 JDK 动态代理模式实现了消费者对服务提供者的 "无感知" 调用。消费者通过动态代理实现了对服务提供者的调用，当调用这些代理对象的方法时，实际上是通过网络向远程服务器发送请求，大大简化了远程服务的使用。

**2）工厂模式：**

在设计 RPC 框架时，我通过工厂模式实现了序列化器工厂、注册中心工厂、服务代理工厂、负载均衡器工厂、重试策略工厂、容错策略工厂等。通过调用工厂类的方法来获取对象，而不是直接通过 new 操作符创建对象，这种方式降低了调用方和具体类之间的耦合度。

**3）双检锁单例模式：**

对于加载指定类型的实例、RPC 配置初始化等操作，我通过对实例进行两次非空的检查和一次对类的 Class 对象进行 synchronized 上锁，实现了双检锁单例模式。这样做的好处是在多线程环境下确保只有一个实例被创建，也可以避免在程序启动时就创建实例（按需加载），从而节省了资源并提高了性能。

**4）装饰者模式：**

为了解决粘包半包的问题，我使用了 Vert.x 内置 RecordParser。但由于消息体的长度是不固定的，所以我要通过调整 RecordParser 的固定长度（变长）来解决，将读取完整的消息拆分为 2 次，先获取请求体的长度，再根据长度完整读取请求体。

由于代码较为复杂，我运用了装饰者模式，新写一个 Wrapper 类来实现上述逻辑。使用时只需要用 Wrapper 封装原有 Vert.x 的 TCP 处理器，就能解决粘包半包。不用修改原始处理器，使得系统能够更灵活地扩展，代码更利于维护。

---

# 练习题

## 1. 在实现RPC框架时，使用工厂模式的主要目的是什么？()

- A. 降低调用方和具体类之间的耦合度
- B. 提高代码的执行效率
- C. 强制所有对象必须通过工厂创建
- D. 减少代码的行数



## 2. 双检锁单例模式相比普通单例模式的主要优势是什么？()

- A. 完全避免了多线程并发问题
- B. 在保证线程安全的同时提高了性能
- C. 减少了代码的复杂度
- D. 强制在程序启动时就创建实例



## 3. 装饰者模式在解决粘包半包问题时的核心优势是什么？()

- A. 可以完全替代原有的处理器
- B. 不需要修改原有代码就能扩展功能
- C. 比其他模式实现更简单
- D. 可以自动解决所有网络问题



## 4. 动态代理模式在RPC框架中最主要解决了什么问题？()

- A. 提高了网络传输速度
- B. 简化了远程服务的使用
- C. 减少了服务端的代码量
- D. 自动处理了所有异常情况



## 5. 关于设计模式的应用，以下哪种说法是错误的？()

- A. 设计模式可以提升代码的可维护性
- B. 设计模式总是能提高代码执行效率
- C. 设计模式需要根据具体场景选择使用
- D. 过度使用设计模式可能导致代码复杂化


---

# 答案解析

**1. 正确答案：A**

官方解析：工厂模式的主要目的是通过工厂类来创建对象，而不是直接使用new操作符，这样可以降低调用方和具体实现类之间的耦合度，使得系统更加灵活和易于扩展。

**知识点：** 设计模式、工厂模式、耦合度

**2. 正确答案：B**

官方解析：双检锁单例模式通过两次非空检查和一次同步锁，既保证了多线程环境下的线程安全，又避免了每次获取实例都需要同步的性能开销，同时还实现了按需加载。

**知识点：** 性能优化、多线程、单例模式、设计模式

**3. 正确答案：B**

官方解析：装饰者模式通过包装原有对象来扩展功能，不需要修改原有代码，使得系统能够更灵活地扩展，代码更利于维护。

**知识点：** 装饰者模式、设计模式、代码扩展性

**4. 正确答案：B**

官方解析：动态代理模式让消费者通过代理对象调用服务，实际是通过网络向远程服务器发送请求，使得远程服务的使用就像调用本地方法一样简单。

**知识点：** RPC框架、代理模式、设计模式

**5. 正确答案：B**

官方解析：设计模式的主要目的是提高代码的可维护性和扩展性，并不总是能提高执行效率，有时甚至会因为增加了抽象层而降低效率。需要根据具体场景合理选择使用。

**知识点：** 代码优化、设计模式、软件设计原则
